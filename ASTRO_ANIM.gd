extends AnimatedSprite

#ALEJANDRO (Feb-14-2020)
#basic script, probably safe bet to keep in case
#editor has it set otherwise. Might be a good habit
#to start to do so editor settings don't have to be
#relied on as heavily


func _ready():
	self._set_playing(true)
